#include "main.h"
#include <stdio.h>

/**
 * main - function that computes the absolute value of an integer.
 *
 * Return: Always 0.
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return  (n);
	}
	return (-n);
}
